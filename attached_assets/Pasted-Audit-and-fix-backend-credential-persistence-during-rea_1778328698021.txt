Audit and fix backend credential persistence during real-device unlock/setup. Do not add new features.

Context:
Manual Expo Go testing found:

* Create decoy entry showed “session expired”
* Security & Devices showed a red cloud warning
  A small UI/message fix was applied:
* missing backend credentials now produce honest “not signed in to backend” messaging
* no crypto/schema/auth weakening was done

But the deeper issue remains:
After normal setup/unlock, the device may still not have backend credentials saved, so security features that require backend auth cannot work.

Goal:
Verify whether the app actually registers/logs in to the backend during real-device setup/unlock and persists credentials via setCredentials().

Tasks:

1. Inspect the current setup/unlock backend credential flow:

   * app/(tabs)/index.tsx
   * screens/SeedSetupScreen.tsx if relevant
   * lib/query-client.ts
   * lib/credentials.ts
   * components/security/api.ts
2. Confirm exactly when apiRequest('/api/auth/register') is called.
3. Confirm exactly when apiRequest('/api/auth/login') is called.
4. Confirm whether setCredentials({ userId, authHash }) is called on successful register/login.
5. Confirm whether the client authHash used for login matches the authHash used during registration.
6. Confirm whether username derivation is stable across setup/unlock.
7. Confirm whether backend failures are silently swallowed in a way that leaves the user unable to use security features.
8. Add safe dev-only diagnostics if needed, but do not log secrets:

   * may log success/failure labels
   * may log endpoint name and status only
   * must not log username, authHash, userId, salt, passwords, headers, session tokens, encryptedBlob, or key material
9. If a bug is found, apply the smallest safe fix so successful setup/unlock stores backend credentials.
10. Do not make backend connectivity required for local vault unlock.
11. Do not block local vault use if backend is down.
12. Do not change crypto or auth architecture.
13. Run TypeScript.
14. Run backend login/register smoke tests if practical.
15. Report any remaining manual test steps.

Critical constraints:

* Do NOT modify cryptography.
* Do NOT modify key derivation.
* Do NOT modify encryption formats.
* Do NOT modify vault entry formats.
* Do NOT modify recovery flows.
* Do NOT modify biometric logic.
* Do NOT modify database schema.
* Do NOT weaken backend auth/session checks.
* Do NOT make cloud/backend login required for local vault use.
* Do NOT implement search/sort.
* Do NOT implement tombstones.
* Do NOT implement password rotation.
* Do NOT add Autofill back.
* Do NOT refactor unrelated systems.
* Do NOT log authHash, passwords, salts, recovery keys, session tokens, headers, encryptedBlob, userId, or key material.

Output:

* Files inspected
* Whether backend register/login is actually called on device flow
* Whether credentials are persisted
* Root cause if credentials are missing
* Files changed
* Exact fixes applied
* TypeScript result
* Backend smoke test result
* Manual retest steps for Create Decoy and Security & Devices
