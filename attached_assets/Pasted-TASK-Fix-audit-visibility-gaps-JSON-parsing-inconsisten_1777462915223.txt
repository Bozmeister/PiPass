TASK: Fix audit visibility gaps + JSON parsing inconsistency

FILES:
- server/index.ts (or main Express entrypoint)
- server/routes.ts

CONSTRAINTS (MANDATORY):

DO NOT modify database schema
DO NOT modify crypto logic
DO NOT change existing endpoint behavior (only extend)
DO NOT break existing validation patterns
ALL changes must be fail-open (never block legitimate users)

--------------------------------------------------

T001 — Add global JSON body parsing

In server entrypoint (where Express app is created):

Add:

app.use(express.json({ limit: "1mb" }))

Place it BEFORE route registration.

KEEP existing urlencoded parser.

Ensure:
- No duplicate parsing
- No breaking existing endpoints

--------------------------------------------------

T002 — Add audit logging for failed login attempts

In ALL login paths:

- Password login
- Passkey login
- TOTP login (if applicable)

On ANY authentication failure:

Add audit event:

action: "login_failed"

Rules:

- NEVER include:
  - password
  - credential_id
  - public_key
  - raw error messages

- Metadata allowed:
  - username (if provided)
  - reason category (SAFE ENUM ONLY):
    - invalid_credentials
    - user_not_found
    - rate_limited
    - step_up_required

- Use EXISTING audit logging function
- MUST be fire-and-forget (never throw)

--------------------------------------------------

T003 — Integrate failed login into security model

On repeated login_failed events:

- Feed into EXISTING anomaly system
- Reuse existing helper (e.g. recordAnomaly or equivalent)

Rules:

- DO NOT create new anomaly system
- DO NOT modify thresholds globally

Behavior:

- Multiple failures → elevate securityLevel
- May trigger soft lock or recovery mode (existing logic)

--------------------------------------------------

T004 — Prevent audit spam (dedupe)

For login_failed:

- Deduplicate per:
  (userId OR username) + IP + 60-second window

Implementation:

- Reuse existing audit dedupe helper if available
- Otherwise:
  lightweight in-memory Map with TTL

Goal:
- Prevent attacker flooding audit logs
- Still retain signal

--------------------------------------------------

T005 — Normalize failure responses

Ensure ALL login failures return:

HTTP 401
Body:
{ "error": "Invalid credentials" }

EXCEPT:
- Rate limit → 429

DO NOT leak:
- user existence
- passkey presence
- TOTP enabled state

--------------------------------------------------

T006 — Preserve uniform timing

Ensure login_failed path still uses:

uniformLoginDelay()

Apply AFTER:
- validation
- rate-limit check

Goal:
- prevent timing attacks

--------------------------------------------------

T007 — Extend audit visibility (optional but recommended)

For rate limit hits on login:

Add audit event:

action: "login_rate_limited"

Metadata:
- ip
- username (if provided)

--------------------------------------------------

VERIFICATION CHECKS:

1. JSON requests to ALL endpoints work (no 400 due to parsing)
2. Failed login attempts generate audit logs
3. Audit logs contain NO sensitive data
4. Multiple failures escalate securityLevel
5. Audit logs do NOT flood under attack (dedupe works)
6. Response timing remains uniform
7. Existing login success flows unchanged

--------------------------------------------------

SUCCESS CRITERIA:

- No schema changes
- No crypto changes
- No breaking API contracts
- Full backward compatibility
- Clean type-check
- Backend boots cleanly